"use client";

// Third party design
import { Spinner as GeistSpinner } from "@geist-ui/core";

/**
 * Throbber (circle) loading animation
 *
 * Usage: when something is being processed in the background
 *
 * @public
 */
export default function Spinner() {
  return <GeistSpinner />;
}
